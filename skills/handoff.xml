> TL;DR: Skills audit complete. All renames, description fixes, and body fixes landed. Uncommitted — needs commit.

<completed>
<!-- Session: 2026-03-04 skills audit -->
- Audited all 21 skills, identified overlaps and description issues
- Deleted `mirror` skill (unused)
- Renamed `newcomer` → `casey-newcomer`, updated description
- Renamed `wtf` → `casey-wtf`, updated description
- Renamed `perf-check` → `casey-perfcheck`, updated description
- Renamed `devils-advocate` → `challenge`, updated description
- Updated `commit` description (was implementation detail, now trigger-oriented)
- Renamed `explain` → `explainCode`, merged description (pedagogical + conceptual)
- Fixed `bugfix` empty args handling (fallback: uncommitted changes, last commit, or ask)
- Fixed `qc` "recent changes" ambiguity (explicit git diff strategy)
- `learn` description reviewed — kept as-is (Tom's call)
- Fixed `sketch` hardcoded `aesthetic/SKILL.md` path → name-based discovery
</completed>

<backlog>
- **Consider bundled resources** — no skill has scripts/, references/, or assets/. `casey-perfcheck` could bundle a benchmark script, `sketch` could bundle a template.
  [status]: idea only, not discussed with Tom
</backlog>

<context>
- Skills live in `/Users/tom/code/ai/skills/`
- Every skill is a bare SKILL.md (no bundled resources)
- `casey-*` is the namespace for opinionated code review personas: casey (perf), casey-newcomer (arch), casey-wtf (trace), casey-perfcheck (quantitative)
- Built-in `/review` is PR-specific (gh pr diff), doesn't overlap with casey-* skills
- `simplify` is a plugin skill (not Tom's) — overlaps with newcomer but distinct (surgical vs holistic)
- Tom confirmed he uses double-check, qc, and learn extensively
</context>
